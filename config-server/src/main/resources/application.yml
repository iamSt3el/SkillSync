spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/iamSt3el/skillsync-config.git
          default-label: main
          clone-on-start: true
server:
  port: 8888